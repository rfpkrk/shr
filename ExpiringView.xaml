<Page x:Class="ChemicalWarehouseWPF.Views.ExpiringView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Истекают"
      Background="#F5F5F5">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Показать за:" VerticalAlignment="Center" Foreground="#666" Margin="0,0,8,0"/>
            <ComboBox x:Name="cmbDays" Width="80" Height="35" SelectedIndex="2" 
                      SelectionChanged="CmbDays_SelectionChanged"
                      BorderBrush="#E0E0E0" BorderThickness="1" Background="White">
                <ComboBoxItem Content="7"/>
                <ComboBoxItem Content="14"/>
                <ComboBoxItem Content="30"/>
                <ComboBoxItem Content="60"/>
            </ComboBox>
            <TextBlock Text="дней" VerticalAlignment="Center" Foreground="#666" Margin="5,0,0,0"/>
        </StackPanel>
        <DataGrid Grid.Row="1" x:Name="dgExpiring" AutoGenerateColumns="False" IsReadOnly="True"
                  Background="White" BorderBrush="#E0E0E0" BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Реагент" Binding="{Binding ReagentName}" Width="150"/>
                <DataGridTextColumn Header="Партия" Binding="{Binding BatchNumber}" Width="100"/>
                <DataGridTextColumn Header="Кол-во" Binding="{Binding CurrentQuantity}" Width="70"/>
                <DataGridTextColumn Header="Ед." Binding="{Binding UnitSymbol}" Width="50"/>
                <DataGridTextColumn Header="Срок" Binding="{Binding ExpirationDate, StringFormat=dd.MM.yyyy}" Width="90"/>
                <DataGridTextColumn Header="Дней" Binding="{Binding DaysLeft}" Width="60"/>
                <DataGridTextColumn Header="Место" Binding="{Binding LocationCode}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>